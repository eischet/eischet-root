package com.eischet.root.app;

/**
 * This class exists ONLY so that the app tooling can load it to check whether the app is in beta or not.
 */
@SuppressWarnings("unused")
public class BetaVersion {
}
